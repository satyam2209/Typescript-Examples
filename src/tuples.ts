let random:[string, number, string, boolean?] = ["abc", 123, "hello"];

// if we want to push new another type of value in random variable Then add in last type as well as questionMark

random.push(true)

// random[0] = 12;